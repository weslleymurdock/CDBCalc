import { ExtractCurrencyValueDirective } from './extract-currency-value.directive';

describe('ExtractCurrencyValueDirective', () => {
  it('should create an instance', () => {
    const directive = new ExtractCurrencyValueDirective();
    expect(directive).toBeTruthy();
  });
});
